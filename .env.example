# Copy to .env and fill values (mobile/desktop/dev).
# NOTE (web deployments): Prefer `--dart-define=...` at build time.
# Some CDNs/WAFs (e.g. Cloudflare) block requests to `.env` assets, which can
# prevent Supabase from initializing on custom domains.
#
# Example:
# flutter build web --release \
#   --dart-define=SUPABASE_URL=... \
#   --dart-define=SUPABASE_ANON_KEY=... \
#   --dart-define=APP_NAME=Dabbler \
#   --dart-define=ENVIRONMENT=production
#
# Keep .env out of source control if it contains secrets.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_PUBLISHABLE_KEY=
APP_NAME=
ENVIRONMENT=development

# Required for Google Sign-In on Flutter Web (Chrome)
# Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs -> (Web application) -> Client ID
GOOGLE_WEB_CLIENT_ID=
